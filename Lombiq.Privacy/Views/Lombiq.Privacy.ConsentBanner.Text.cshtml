@using OrchardCore.Settings
@inject ISiteService SiteService

@{
    var consentBannerText = (await SiteService.GetCustomSettingsAsync(TypeNames.PrivacyConsentBannerSettings))
        .GetOrCreate<LiquidPart>()
        .Liquid;
}

@await Orchard.LiquidToHtmlAsync(consentBannerText)

