@inject OrchardCore.Settings.ISiteService SiteService

@{
    var consentBannerText = (await SiteService.GetSiteSettingsAsync())
        .As<ContentItem>(TypeNames.PrivacyConsentBannerSettings).GetOrCreate<LiquidPart>()?.Liquid;
}

@await Orchard.LiquidToHtmlAsync(consentBannerText)

