@using OrchardCore.Settings
@inject ISiteService SiteService

@{
    var consentCheckboxText = (await SiteService.GetCustomSettingsAsync(TypeNames.PrivacyConsentCheckboxSettings))
        .GetOrCreate<LiquidPart>()
        .Liquid;
}

@await Orchard.LiquidToHtmlAsync(consentCheckboxText)
