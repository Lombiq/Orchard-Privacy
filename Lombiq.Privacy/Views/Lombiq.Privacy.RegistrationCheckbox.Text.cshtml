@using OrchardCore.Settings
@inject ISiteService SiteService

@{
    var registrationConsentText = (await SiteService.GetCustomSettingsAsync(TypeNames.PrivacyRegistrationConsentSettings))
        .GetOrCreate<LiquidPart>()
        .Liquid;
}

@await Orchard.LiquidToHtmlAsync(registrationConsentText)
