name: Lint Script and Style

# Runs for PRs opened for any branch, and pushes to the main branch.
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint:
    name: Lint Script and Style
    uses: Lombiq/NodeJs-Extensions/.github/workflows/lint.yml@issue/OCC-314
    with:
      style-css-paths: Lombiq.Privacy
