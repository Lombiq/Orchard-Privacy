@inject OrchardCore.Settings.ISiteService _siteService

@{
    var consentCheckboxText = (await _siteService.GetSiteSettingsAsync())
        .As<ContentItem>(TypeNames.ConsentCheckboxSettings).As<LiquidPart>()?.Liquid;
}

@await Orchard.LiquidToHtmlAsync(consentCheckboxText)
