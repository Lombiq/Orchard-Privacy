@inject OrchardCore.Settings.ISiteService _siteService

@{
    var consentBannerText = (await _siteService.GetSiteSettingsAsync())
        .As<ContentItem>(TypeNames.ConsentBannerSettings).As<LiquidPart>()?.Liquid;
}

@await Orchard.LiquidToHtmlAsync(consentBannerText)

